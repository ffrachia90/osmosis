{
  "mcpServers": {
    "osmosis": {
      "command": "node",
      "args": [
        "/absolute/path/to/osmosis/dist/mcp/server.js"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "sk-ant-...",
        "OPENAI_API_KEY": "sk-..."
      }
    }
  }
}

// Notes:
// - ANTHROPIC_API_KEY: REQUIRED (for Claude 3.5 Sonnet - code generation)
// - OPENAI_API_KEY: OPTIONAL (for RAG embeddings - better component detection)
//   If not provided, uses local embeddings (TF-IDF)
// - Replace "/absolute/path/to/osmosis" with your actual path

